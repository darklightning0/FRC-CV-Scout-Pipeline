# Cloudflare Pages — bindings are managed here (dashboard Bindings UI is locked).
# Root directory in Pages should be `web/` so this file is used on deploy.

name = "frc-cv-scout-pipeline"
compatibility_date = "2025-01-01"
pages_build_output_dir = "dist"

# KV namespace: frc_huntereyes
# https://dash.cloudflare.com → Workers KV → frc_huntereyes
[[kv_namespaces]]
binding = "CV_TELEMETRY"
id = "40a03f1e396c4afdbf542f872698bd3c"
preview_id = "40a03f1e396c4afdbf542f872698bd3c"
